specVersion: "v1beta"
serviceId: "zenflow"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "zenflow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
